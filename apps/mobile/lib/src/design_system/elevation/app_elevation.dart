import 'package:flutter/material.dart';

/// Standardized elevation/shadow constants for LifeCircle OS.
class AppElevation {
  const AppElevation._();

  /// No elevation
  static const double none = 0.0;
  
  /// Level 1 elevation (e.g. cards)
  static const double level1 = 1.0;
  
  /// Level 2 elevation (e.g. floating action buttons)
  static const double level2 = 3.0;
  
  /// Level 3 elevation (e.g. dialogs, bottom sheets)
  static const double level3 = 6.0;
  
  /// Level 4 elevation
  static const double level4 = 8.0;
  
  /// Level 5 elevation
  static const double level5 = 12.0;
}
